version: "3"
services:
  deep-look-server:
    container_name: deep-look-server
    image: tmfrk0426/deep-look-server
    env_file:
      - "./.env"
    environment:
      - TZ=Asia/Seoul
    hostname: deep-look-server
    expose:
      - "8080"
    restart: always

#  flask-server:
#    container_name: flask-server
#    image: tmfrk0426/flask-server
##    env_file:
##      - "./.env"
#    environment:
#      - TZ=Asia/Seoul
#    hostname: flask-server
#    expose:
#      - "6000"
#    restart: always
#    build:
#      dockerfile: Dockerfile
#      context: ./flask-server


  nginx:
    depends_on:
      - deep-look-server
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./nginx
    ports:
      - "80:80"
    environment:
      - TZ=Asia/Seoul